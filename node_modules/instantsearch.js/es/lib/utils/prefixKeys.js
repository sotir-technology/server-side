import mapKeys from 'lodash/mapKeys';

function prefixKeys(prefix, obj) {
  if (obj) {
    return mapKeys(obj, function (_0, key) {
      return prefix + key;
    });
  }

  return undefined;
}

export default prefixKeys;